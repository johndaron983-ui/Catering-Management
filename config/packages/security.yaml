security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        # Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
        App\Entity\User:
            algorithm: auto
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username
        # used to reload user from session & other features (e.g. switch_user)
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        api:
            pattern: ^/api
            stateless: true
            provider: app_user_provider
            json_login:
                check_path: /api/login
                username_path: username
                password_path: password
                success_handler: App\Security\JWTAuthenticationSuccessHandler
                failure_handler: lexik_jwt_authentication.handler.authentication_failure
            jwt: ~
        main:
            lazy: true
            provider: app_user_provider

            logout:
                path: app_logout
            custom_authenticator: 
                - App\Security\LoginAuthenticator
                - App\Security\GoogleAuthenticator
                # where to redirect after logout
                # target: app_any_route

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # Public API endpoints
        - { path: ^/api/register, roles: PUBLIC_ACCESS }
        - { path: ^/api/login, roles: PUBLIC_ACCESS }
        - { path: ^/api/google-login, roles: PUBLIC_ACCESS }
        - { path: ^/api/google-debug, roles: PUBLIC_ACCESS }
        - { path: ^/api/resend-verification-email, roles: PUBLIC_ACCESS }
        - { path: ^/api/verify-email, roles: PUBLIC_ACCESS }
        - { path: ^/api/mobile/health, roles: PUBLIC_ACCESS }

        # All other API routes require a valid JWT
        - { path: ^/api, roles:  }

        # Shared admin area exceptions must be before the catch-all ^/admin rule
        - { path: ^/admin/inventory, roles: ["ROLE_ADMIN", "ROLE_STAFF"] }

        # Admin-only routes
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/users, roles: ROLE_ADMIN }
        - { path: ^/verify-email, roles: PUBLIC_ACCESS }
        - { path: ^/activity-logs, roles: ROLE_ADMIN }
        - { path: ^/bookings, roles: ["ROLE_ADMIN", "ROLE_STAFF"] }
        - { path: ^/admin/services, roles: ["ROLE_ADMIN", "ROLE_STAFF"] }
        - { path: ^/inventory, roles: ["ROLE_ADMIN", "ROLE_STAFF"] }
        - { path: ^/product, roles: ["ROLE_ADMIN", "ROLE_STAFF"] }
        - { path: ^/supplier, roles: ["ROLE_ADMIN", "ROLE_STAFF"] }
        - { path: ^/profile, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
